import { DISCO_THEME } from "./disco";

// Swap this one export to change the app's visual system and artwork.
export const THEME = DISCO_THEME;

export type { MixTheme, UiPreviewTheme, RoundAsset } from "./types";
export { DISCO_THEME };
