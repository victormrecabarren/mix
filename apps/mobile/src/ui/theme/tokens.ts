import { THEME } from "./index";

// Back-compat export for existing components. New theme-level values
// should be added to `disco.ts` and consumed through THEME or this alias.
export const v1 = THEME;

export { THEME };
