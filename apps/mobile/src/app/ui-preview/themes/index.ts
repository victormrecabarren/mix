import { DISCO_THEME } from "./disco";

// Swap this one export to change the preview's visual system and artwork.
export const THEME = DISCO_THEME;

export type { UiPreviewTheme, RoundAsset } from "./types";
export { DISCO_THEME };
