import { THEME } from "./themes";

// Back-compat export for existing preview components. New theme-level values
// should be added to `themes/disco.ts` and consumed through THEME or this alias.
export const v1 = THEME;
